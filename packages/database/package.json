{
  "name": "@rag-brain/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.12.1"
  },
  "devDependencies": {
    "@rag-brain/tsconfig": "workspace:*",
    "prisma": "^5.12.1",
    "typescript": "^5.4.5"
  }
}
