export const REFRESH_TOKEN_ERRORS = {
  NOT_FOUND: 'Refresh token not found',
  EXPIRED: 'Refresh token is expired',
  INVALID: 'Invalid refresh token',
  MALFORMED: 'Malformed refresh token',
};

export const REFRESH_TOKEN_EXPIRATION_DAYS = 7;
export const BCRYPT_SALT_ROUNDS = 10;
export const REFRESH_TOKEN_BYTES = 32;
export const TOKEN_DELIMITER = '.';
export const TOKEN_ID_INDEX = 0;
export const TOKEN_RAW_INDEX = 1;
export const HEX_ENCODING = 'hex';
